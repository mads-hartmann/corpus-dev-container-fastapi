# Corpus Dev Container FastAPI

Minimal FastAPI starter app based on the canonical tutorial structure.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
fastapi dev app/main.py
```

Open the printed local URL and visit `/` to see the JSON response.
